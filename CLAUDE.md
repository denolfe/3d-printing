# Printing Project

## Workflow

- Use the `openscad` skill for creating and modifying 3D models
- Export STL and open in Bambu Studio: `open -a "BambuStudio" <file>.stl`

## OpenSCAD Preferences

- High circle resolution: `$fn = 256`
